---
<<<<<<< HEAD
- name: NMAP hosts and report
  hosts: nmap_servers
  remote_user: ubuntu
  become: true 
  become_method: sudo

  tasks:

  - name: Install NMAP
    apt:
      name: nmap
      state: latest
      update_cache: True

  - name: Copy nmap targets list
    copy:
      src: ./targets.txt
      dest: ~/targets.txt
      force: yes
      mode: 0644

  - name: Run nmap
    shell: "nmap -iL ~/targets.txt -p 80 -oG -"
    register: nmap_result

  - name: Nmap output
    debug:
      msg: "{{ nmap_result }}"
=======
- name: install nginx and index
  hosts: localhost

  tasks:
  - name: Show value of var
    debug:
      msg: "{{ ansible_facts['os_family'] }}"

  - name: Install Nginx web server
    apt:
      name: nginx
      state: present
    notify: Restart Nginx
    tags:
      - nginx
    when: ansible_facts['os_family'] != "Darwin"
  
  - name: copy index
    copy:
      src: index.html
      dest: /var/www/html/index.html
      mode: "0644"
    when: ansible_facts['os_family'] != "Darwin"

  - name: install nginx for macOS
    community.general.homebrew:
      name: nginx
      state: present
  
>>>>>>> 15efb2d0ceb1aca568cc096de3edba8a63c8cf2c
